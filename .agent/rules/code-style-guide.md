---
trigger: always_on
---

- Don't introduce unnecessary state
- Prefer a functional style over an imperative one
- Avoid hacky solutions using setTimeout or requestAnimationFrame
- Don't comment just to describe what you did. Leave comment's only if trully necessary
- Write code as a senior developer would, following best practices
- Stick to simple solutions
- Use arrow functions instead of the function keyword
- Use type instead of interface
- Prioritize design consistency. Don't introduce new styles like paddings, colors or font sizes if we can reuse existing
- Dont use inline styles. Use .css files
