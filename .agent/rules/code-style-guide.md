---
trigger: always_on
---

- Write minimal amount of code
- Use arrow functions instead of the function keyword
- Use type instead of interface
- Prioritize design consistency. Don't introduce new styles like paddings, colors or font sizes if we can reuse existing
- Dont use inline styles. Use .css files
