{
  "name": "pwas-monorepo",
  "private": true,
  "scripts": {
    "build:mono": "npm run build --workspace=mono",
    "build:goread": "npm run build --workspace=goread"
  },
  "workspaces": [
    "mono",
    "goread",
    "ui"
  ]
}