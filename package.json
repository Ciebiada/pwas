{
  "name": "pwas-monorepo",
  "private": true,
  "scripts": {
    "build:mono": "npm run build --workspace=mono",
    "build:readium": "npm run build --workspace=readium"
  },
  "workspaces": [
    "mono",
    "readium",
    "ui"
  ]
}