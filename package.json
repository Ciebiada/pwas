{
  "name": "pwas-monorepo",
  "private": true,
  "scripts": {
    "build:mono": "npm run build --workspace=mono",
    "build:readium": "npm run build --workspace=readium",
    "check": "biome check --write .",
    "test:e2e:mono": "playwright test --config=playwright.config.ts"
  },
  "workspaces": [
    "mono",
    "readium",
    "ui"
  ],
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@biomejs/biome": "2.3.12",
    "typescript": "^5.7.2"
  }
}
